---
const { link, image, description } = Astro.props;
---

<figure class="w-[320px] h-40 bg-white rounded-xl border border-gray-200 p-3 flex flex-row items-center gap-4 shadow-sm hover:shadow-md transition-shadow">
  <a href={link} target="_blank" rel="noopener noreferrer" class="shrink-0">
    <img
      class="w-40 h-28 object-contain rounded-md border border-gray-200"
      src={image}
      alt={description}
      loading="lazy"
    />
  </a>

  <figcaption class="text-gray-800 font-medium text-sm text-center flex-1">
    {description}
  </figcaption>
</figure>

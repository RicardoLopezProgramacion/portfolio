---
const { href, text, class: customClass = '', target = '_blank' } = Astro.props;
---

<a
  href={href}
  target={target}
  rel={target === '_blank' ? 'noopener noreferrer' : undefined}
  class={`inline-block px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600 transition ${customClass}`}
>
  {text ? text : href}
</a>
